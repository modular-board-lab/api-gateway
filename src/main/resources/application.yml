server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: ${services.auth-service.url:http://localhost:8082}
          predicates:
            - Path=/auth,/auth/**
        - id: board-service-boards
          uri: ${services.board-service.url:http://localhost:8081}
          predicates:
            - Path=/boards,/boards/**
        - id: board-service-posts
          uri: ${services.board-service.url:http://localhost:8081}
          predicates:
            - Path=/posts,/posts/**
        - id: board-query-service-posts
          uri: ${services.board-query-service.url:http://localhost:8084}
          predicates:
            - Path=/bff/posts,/bff/posts/**
        - id: file-service
          uri: ${services.file-service.url:http://localhost:8083}
          predicates:
            - Path=/files,/files/**

services:
  auth-service:
    url: http://localhost:8082
  board-service:
    url: http://localhost:8081
  board-query-service:
    url: http://localhost:8084
  file-service:
    url: http://localhost:8083

auth:
  jwt:
    issuer: auth-service
    secret: ${JWT_SECRET:local-dev-secret-local-dev-secret-local-dev-secret}

security:
  public-paths:
    - method: POST
      path: /auth/signup
    - method: POST
      path: /auth/login
    - method: POST
      path: /auth/reissue
    - method: GET
      path: /actuator/health
    - method: GET
      path: /boards/**
    - method: GET
      path: /posts/**
    - method: GET
      path: /bff/posts/**
    - method: GET
      path: /files/**
    - method: GET
      path: /posts/*/comments

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
  endpoint:
    health:
      show-details: always
